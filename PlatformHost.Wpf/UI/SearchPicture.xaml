<Page x:Class="WpfApp2.UI.SearchPicture"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.UI"
      xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="1920" d:DesignWidth="1080"
      Title="SearchPicture">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="600"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        

        <ItemsControl Grid.Column="0" Grid.Row="0" ItemsSource="{Binding DefectTypes}" HorizontalAlignment="Left" Margin="20,0,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Width="100" Height="100" Margin="5" Click="DefectButton_Click" Tag="{Binding}">
                        <StackPanel>
                            <Image Source="{Binding ImagePath}" Stretch="Uniform" Height="70"/>
                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" FontSize="25"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 添加文字注释 -->
        <TextBlock  Grid.Column="0" Grid.Row="1" Text="每种瑕疵会有多种参数表征，请在TAB栏选择，多种表征会相互叠加" 
               FontSize="20" Foreground="Yellow" HorizontalAlignment="Left" Margin="20,0,0,0" VerticalAlignment="Bottom"/>

        <!-- TabControl 和 WpfPlot -->
        <Grid  Grid.Column="0" Grid.Row="2" Margin="20">
            <TabControl x:Name="PlotTabControl" 
            TabStripPlacement="Left" 
            SelectionChanged="PlotTabControl_SelectionChanged">
                <!-- 移除静态 TabItem，内容将由代码填充 -->
            </TabControl>
        </Grid>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        </Grid>

        <!-- 图片显示区域 -->
        <ItemsControl Grid.Column="1" Grid.Row="0" x:Name="ImageItemsControl">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" Rows="2"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="5">
                        <Image Source="{Binding}" Stretch="Uniform" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button  Grid.Column="1" Grid.Row="1" Width="100" Height="100" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10" Click="ClearImage_Click">
            <Image Source="UI/clear.png" Stretch="Uniform" />
        </Button>
        <!-- 分页控制 -->
        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Height="100"  Margin="10">
            <Button Width="100" Height="70" Margin="5" Click="PreviousPage_Click">
                <Image Source="E:\WpfTest\WpfApp2\UI\fore_page.png" Stretch="Uniform" />
            </Button>
            <TextBlock Text="{Binding PageInfo}"  FontSize="30" Foreground="White" VerticalAlignment="Center" Margin="5"/>
            <Button Width="100" Height="70" Margin="5" Click="PreviousPage_Click">
                <Image Source="E:\WpfTest\WpfApp2\UI\next_page.png" Stretch="Uniform"/>
            </Button>
        </StackPanel>
        <Button Grid.Column="1" Grid.Row="1" Width="100" Height="100" HorizontalAlignment="Right"  VerticalAlignment="Bottom" Click="Exit_Click" Margin="10">
            <Image Source="E:\WpfTest\WpfApp2\UI\exit.png" Stretch="Uniform"/>
        </Button>

    </Grid>
</Page>
