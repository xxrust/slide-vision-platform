<Window x:Class="WpfApp2.UI.AutoDeleteImageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="自动删图配置" Height="677" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF1E1E1E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="🗑️ 自动删图配置"
                   FontSize="24" FontWeight="Bold"
                   Foreground="White" HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- 目标文件夹显示 -->
        <Border Grid.Row="1" Background="#FF2A2A2A"
                BorderBrush="#FF444444" BorderThickness="1"
                CornerRadius="5" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="删除目标文件夹："
                          Foreground="#FF888888" FontSize="12"
                          Margin="0,0,0,5"/>
                <TextBlock x:Name="TargetFolderText"
                          Text="运行目录/原图存储"
                          Foreground="#FF00D4FF" FontSize="14"
                          FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <!-- 策略1：按天数删除 -->
        <Border Grid.Row="2" Background="#FF2A2A2A"
                BorderBrush="#FF444444" BorderThickness="1"
                CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <CheckBox x:Name="EnableDeleteByDaysCheckBox"
                         Foreground="White" FontSize="14" FontWeight="Bold"
                         Checked="EnableDeleteByDaysCheckBox_Checked"
                         Unchecked="EnableDeleteByDaysCheckBox_Unchecked">
                    <TextBlock>
                        <Run Text="策略1：按天数删除" Foreground="#FF00FF00"/>
                    </TextBlock>
                </CheckBox>

                <Grid Margin="25,10,0,0" x:Name="DeleteByDaysPanel" IsEnabled="False">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="删除"
                              Foreground="#FFCCCCCC" FontSize="13"
                              VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <TextBox Grid.Column="1" x:Name="DaysTextBox"
                            Text="7" FontSize="14"
                            Background="#FF333333" Foreground="White"
                            BorderBrush="#FF555555" TextAlignment="Center"
                            VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="2" Text="天前的图片"
                              Foreground="#FFCCCCCC" FontSize="13"
                              VerticalAlignment="Center" Margin="10,0,0,0"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 策略2：按磁盘空间删除 -->
        <Border Grid.Row="3" Background="#FF2A2A2A"
                BorderBrush="#FF444444" BorderThickness="1"
                CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <CheckBox x:Name="EnableDeleteBySpaceCheckBox"
                         Foreground="White" FontSize="14" FontWeight="Bold"
                         Checked="EnableDeleteBySpaceCheckBox_Checked"
                         Unchecked="EnableDeleteBySpaceCheckBox_Unchecked">
                    <TextBlock>
                        <Run Text="策略2：按磁盘空间删除" Foreground="#FFFFAA00"/>
                    </TextBlock>
                </CheckBox>

                <Grid Margin="25,10,0,0" x:Name="DeleteBySpacePanel" IsEnabled="False">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 触发阈值设置 -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="当D盘剩余空间小于"
                                  Foreground="#FFCCCCCC" FontSize="13"
                                  VerticalAlignment="Center" Margin="0,0,10,0"/>

                        <TextBox Grid.Column="1" x:Name="TriggerSpaceTextBox"
                                Text="50" FontSize="14"
                                Background="#FF333333" Foreground="White"
                                BorderBrush="#FF555555" TextAlignment="Center"
                                VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="2" Text="GB 时触发删除"
                                  Foreground="#FFCCCCCC" FontSize="13"
                                  VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </Grid>

                    <!-- 目标空间设置 -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="删除直到剩余空间达到"
                                  Foreground="#FFCCCCCC" FontSize="13"
                                  VerticalAlignment="Center" Margin="0,0,10,0"/>

                        <TextBox Grid.Column="1" x:Name="TargetSpaceTextBox"
                                Text="100" FontSize="14"
                                Background="#FF333333" Foreground="White"
                                BorderBrush="#FF555555" TextAlignment="Center"
                                VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="2" Text="GB"
                                  Foreground="#FFCCCCCC" FontSize="13"
                                  VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </Grid>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 状态信息 -->
        <Border Grid.Row="4" Background="#FF2A2A2A"
                BorderBrush="#FF444444" BorderThickness="1"
                CornerRadius="5" Padding="10" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="状态信息"
                          Foreground="#FF888888" FontSize="12"
                          FontWeight="Bold" Margin="0,0,0,5"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="CurrentDiskSpaceText"
                                  Text="D盘剩余空间：计算中..."
                                  Foreground="#FFCCCCCC" FontSize="12"
                                  Margin="0,0,0,3"/>
                        <TextBlock x:Name="LastCheckTimeText"
                                  Text="上次检测时间：未检测"
                                  Foreground="#FFCCCCCC" FontSize="12"
                                  Margin="0,0,0,3"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock x:Name="AutoCheckStatusText"
                                  Text="自动检测：未启用"
                                  Foreground="#FFCCCCCC" FontSize="12"
                                  Margin="0,0,0,3"/>
                        <TextBlock x:Name="NextCheckTimeText"
                                  Text="下次检测时间：--"
                                  Foreground="#FFCCCCCC" FontSize="12"
                                  Margin="0,0,0,3"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 日志区域 -->
        <Border Grid.Row="5" Background="#FF1A1A1A"
                BorderBrush="#FF444444" BorderThickness="1"
                CornerRadius="5" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="LogTextBox"
                        Background="Transparent" Foreground="#FF888888"
                        FontFamily="Consolas" FontSize="11"
                        TextWrapping="Wrap" IsReadOnly="True"
                        BorderThickness="0"/>
            </ScrollViewer>
        </Border>

        <!-- 操作按钮 -->
        <Grid Grid.Row="6" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" x:Name="ManualExecuteButton"
                   Content="🚀 立即执行"
                   Width="100" Height="35" Margin="0,0,10,0"
                   Background="#FF007BFF" Foreground="White"
                   FontSize="14" FontWeight="Bold"
                   Click="ManualExecuteButton_Click"/>

            <Button Grid.Column="2" x:Name="SaveConfigButton"
                   Content="💾 保存配置"
                   Width="100" Height="35" Margin="0,0,10,0"
                   Background="#FF28A745" Foreground="White"
                   FontSize="14" FontWeight="Bold"
                   Click="SaveConfigButton_Click"/>

            <Button Grid.Column="3" x:Name="StartAutoCheckButton"
                   Content="⏰ 启动自动检测"
                   Width="120" Height="35" Margin="0,0,10,0"
                   Background="#FFFFC107" Foreground="Black"
                   FontSize="14" FontWeight="Bold"
                   Click="StartAutoCheckButton_Click"/>

            <Button Grid.Column="4" x:Name="CloseButton"
                   Content="❌ 关闭"
                   Width="80" Height="35"
                   Background="#FFDC3545" Foreground="White"
                   FontSize="14" FontWeight="Bold"
                   Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>