<Window x:Class="WpfApp2.UI.MotorParameterSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="电机参数设置" Height="594" Width="568"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FF2C3E50">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题区域 -->
        <Border Grid.Row="0" Background="#FF34495E" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="⚙️ 电机参数设置" FontSize="18" FontWeight="Bold" Foreground="#FF3498DB" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="设置电机运动参数" FontSize="12" Foreground="#FFBDC3C7" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 参数设置区域 -->
        <Border Grid.Row="1" Background="#FF34495E" CornerRadius="8" Padding="20">
            <StackPanel>
                <TextBlock Text="📊 运动参数" FontSize="14" FontWeight="Bold" Foreground="#FF3498DB" Margin="0,0,0,15"/>
                
                <!-- 正向坐标 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="正向坐标:" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="ForwardCoordTextBox" Height="30" Margin="5,0"
                             Background="#FF2C3E50" Foreground="White" BorderBrush="#FF3498DB" BorderThickness="1"/>
                    <TextBlock Grid.Column="2" Text="(DM904.F)" FontSize="10" Foreground="#FFBDC3C7" VerticalAlignment="Center"/>
                </Grid>

                <!-- 反向坐标 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="反向坐标:" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="ReverseCoordTextBox" Height="30" Margin="5,0"
                             Background="#FF2C3E50" Foreground="White" BorderBrush="#FF3498DB" BorderThickness="1"/>
                    <TextBlock Grid.Column="2" Text="(DM906.F)" FontSize="10" Foreground="#FFBDC3C7" VerticalAlignment="Center"/>
                </Grid>

                <!-- 启动速度 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="启动速度:" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="StartSpeedTextBox" Height="30" Margin="5,0"
                             Background="#FF2C3E50" Foreground="White" BorderBrush="#FF3498DB" BorderThickness="1"/>
                    <TextBlock Grid.Column="2" Text="(DM100.U)" FontSize="10" Foreground="#FFBDC3C7" VerticalAlignment="Center"/>
                </Grid>

                <!-- 加速度 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="加速度:" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="AccelerationTextBox" Height="30" Margin="5,0"
                             Background="#FF2C3E50" Foreground="White" BorderBrush="#FF3498DB" BorderThickness="1"/>
                    <TextBlock Grid.Column="2" Text="(DM101.U)" FontSize="10" Foreground="#FFBDC3C7" VerticalAlignment="Center"/>
                </Grid>

                <!-- 移动速度 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="移动速度:" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="MoveSpeedTextBox" Height="30" Margin="5,0"
                             Background="#FF2C3E50" Foreground="White" BorderBrush="#FF3498DB" BorderThickness="1"/>
                    <TextBlock Grid.Column="2" Text="(DM102.D)" FontSize="10" Foreground="#FFBDC3C7" VerticalAlignment="Center"/>
                </Grid>

                <!-- 位置负极限 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="位置负极限:" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="NegativeLimitTextBox" Height="30" Margin="5,0"
                             Background="#FF2C3E50" Foreground="White" BorderBrush="#FF3498DB" BorderThickness="1"/>
                    <TextBlock Grid.Column="2" Text="(DM900.D)" FontSize="10" Foreground="#FFBDC3C7" VerticalAlignment="Center"/>
                </Grid>

                <!-- 位置正极限 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="位置正极限:" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="PositiveLimitTextBox" Height="30" Margin="5,0"
                             Background="#FF2C3E50" Foreground="White" BorderBrush="#FF3498DB" BorderThickness="1"/>
                    <TextBlock Grid.Column="2" Text="(DM902.D)" FontSize="10" Foreground="#FFBDC3C7" VerticalAlignment="Center"/>
                </Grid>

                <!-- 状态显示 -->
                <Border Background="#FF2C3E50" CornerRadius="6" Padding="10" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="📡 连接状态" FontSize="12" FontWeight="Bold" Foreground="#FF3498DB" Margin="0,0,0,5"/>
                        <TextBlock x:Name="StatusTextBlock" Text="正在读取参数..." FontSize="11" Foreground="#FFBDC3C7"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="ToPositiveCoordButton" Content="🔺 到正坐标" Width="100" Height="35" Margin="0,0,10,0"
                    Background="#FF2ECC71" Foreground="White" FontSize="12" FontWeight="Bold" Click="ToPositiveCoordButton_Click"/>
            <Button x:Name="ToNegativeCoordButton" Content="🔻 到负坐标" Width="100" Height="35" Margin="0,0,10,0"
                    Background="#FFE67E22" Foreground="White" FontSize="12" FontWeight="Bold" Click="ToNegativeCoordButton_Click"/>
            <Button x:Name="RefreshButton" Content="🔄 刷新参数" Width="100" Height="35" Margin="0,0,10,0"
                    Background="#FF3498DB" Foreground="White" FontSize="12" FontWeight="Bold" Click="RefreshButton_Click"/>
            <Button x:Name="ConfirmButton" Content="✅ 确认修改" Width="100" Height="35" Margin="0,0,10,0"
                    Background="#FF27AE60" Foreground="White" FontSize="12" FontWeight="Bold" Click="ConfirmButton_Click"/>
            <Button x:Name="CancelButton" Content="❌ 取消" Width="80" Height="35"
                    Background="#FF95A5A6" Foreground="White" FontSize="12" FontWeight="Bold" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>