<Window x:Class="WpfApp2.UI.FixedShotTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="定拍测试"
        Height="230"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF2C3E50">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="LOT：" Foreground="White" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="LotTextBox"
                     Grid.Column="1"
                     Height="28"
                     VerticalContentAlignment="Center"
                     TextChanged="LotTextBox_TextChanged"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CaptureButton"
                    Grid.Column="0"
                    Content="抓拍"
                    Height="70"
                    FontSize="18"
                    FontWeight="Bold"
                    Background="#FF27AE60"
                    Foreground="White"
                    Click="CaptureButton_Click"/>

            <Border Grid.Column="2" Background="#FF34495E" CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="抓拍次数" Foreground="#FFBDC3C7" FontSize="12"/>
                    <TextBlock x:Name="CaptureCountTextBlock"
                               Text="0"
                               Foreground="White"
                               FontSize="32"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>

