<Window x:Class="WpfApp2.UI.LoadingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="加载中" Height="150" Width="300"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 加载动画和提示文本 -->
            <StackPanel Grid.Row="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                <!-- 简化的加载图标 -->
                <Grid Width="40" Height="40" Margin="0,0,0,10">
                    <Ellipse Width="40" Height="40" Stroke="#4CAF50" StrokeThickness="4" Opacity="0.3"/>
                    <Ellipse Width="40" Height="40" Stroke="#4CAF50" StrokeThickness="4" 
                             StrokeDashArray="20 10" 
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                                       Storyboard.TargetProperty="Angle"
                                                       From="0" To="360" Duration="0:0:1.5"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>
                
                <TextBlock x:Name="MessageText" Text="正在匹配图片组，请稍候..." 
                          FontSize="14" Foreground="#333" 
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window> 