<Window x:Class="WpfApp2.UI.ComponentEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加组件" Height="387" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 卡片类型显示 -->
        <Border Grid.Row="0" Background="#FFF8F9FA" CornerRadius="8" Padding="15,10" Margin="0,0,0,15">
            <TextBlock x:Name="TypeTextBlock" Text="继电器控制卡片" 
                       FontSize="16" FontWeight="Bold" Foreground="#FF2C3E50" 
                       HorizontalAlignment="Center"/>
        </Border>
        
        <!-- 组件名称 -->
        <Label Grid.Row="1" Content="组件名称:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="NameTextBox" Height="35" Margin="0,0,0,15"
                 FontSize="14" VerticalContentAlignment="Center"
                 BorderBrush="#FF3498DB" BorderThickness="2"
                 Padding="8,0,8,0"
                 ToolTip="请输入组件的描述性名称，如：15°光触发、COR触发、LX-100传感器等"/>
        
        <!-- 寄存器地址 -->
        <StackPanel Grid.Row="3">
            <Label Content="寄存器地址:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock x:Name="RegisterHintTextBlock" Text="示例：R506, MR1915, DM0.L" 
                       FontSize="12" Foreground="#FF7F8C8D" Margin="0,0,0,5"/>
        </StackPanel>
        <TextBox Grid.Row="4" x:Name="RegisterTextBox" Height="35" Margin="0,0,0,15"
                 FontSize="14" VerticalContentAlignment="Center"
                 BorderBrush="#FFF39C12" BorderThickness="2"
                 Padding="8,0,8,0"
                 ToolTip="请输入PLC寄存器地址，确保地址格式正确"/>
        
        <!-- 间隔 -->
        <Grid Grid.Row="5" Height="10"/>
        
        <!-- 按钮 -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="确定" Width="80" Height="35" Margin="0,0,10,0"
                    Background="#FF27AE60" Foreground="White" FontWeight="Bold"
                    Click="OkButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            
            <Button x:Name="CancelButton" Content="取消" Width="80" Height="35"
                    Background="#FF95A5A6" Foreground="White" FontWeight="Bold"
                    Click="CancelButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window> 