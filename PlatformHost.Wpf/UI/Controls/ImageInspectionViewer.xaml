<UserControl x:Class="WpfApp2.UI.Controls.ImageInspectionViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid Background="Black">
        <Border Background="Black">
            <Canvas x:Name="ImageCanvas" ClipToBounds="True" Background="Black">
                <Image x:Name="ImageElement"
                       RenderOptions.BitmapScalingMode="NearestNeighbor"
                       SnapsToDevicePixels="True"
                       Stretch="None"/>
            </Canvas>
        </Border>

        <Border HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="6"
                Background="#AA000000" CornerRadius="4" Padding="6,3">
            <TextBlock x:Name="InfoTextBlock" Foreground="White" FontSize="12" Text="--"/>
        </Border>
    </Grid>
</UserControl>
