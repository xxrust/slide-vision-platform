<UserControl x:Class="WpfApp2.UI.Controls.GenericCameraConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Border Background="#FF2C3E50" CornerRadius="6" Padding="12">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="厂商" Width="60" Foreground="White" VerticalAlignment="Center"/>
                <ComboBox x:Name="VendorComboBox" Width="160" Height="26" Margin="0,0,10,0"/>
                <TextBlock Text="型号" Width="50" Foreground="White" VerticalAlignment="Center"/>
                <TextBox x:Name="ModelTextBox" Width="140" Height="26" Margin="6,0,0,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="序列号" Width="60" Foreground="White" VerticalAlignment="Center"/>
                <TextBox x:Name="SerialTextBox" Width="240" Height="26"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="曝光(us)" Width="70" Foreground="White" VerticalAlignment="Center"/>
                <TextBox x:Name="ExposureTextBox" Width="100" Height="26"/>
                <TextBlock Text="增益" Width="40" Foreground="White" VerticalAlignment="Center" Margin="12,0,0,0"/>
                <TextBox x:Name="GainTextBox" Width="80" Height="26"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="触发源" Width="70" Foreground="White" VerticalAlignment="Center"/>
                <ComboBox x:Name="TriggerSourceComboBox" Width="140" Height="26"/>
                <CheckBox x:Name="TriggerEnableCheckBox" Content="启用触发" Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="触发延时(us)" Width="90" Foreground="White" VerticalAlignment="Center"/>
                <TextBox x:Name="TriggerDelayTextBox" Width="100" Height="26"/>
            </StackPanel>

            <Button x:Name="ApplyButton" Content="应用配置" Height="30" Width="120"
                    Background="#FF3498DB" Foreground="White" FontWeight="Bold"
                    Click="ApplyButton_Click" HorizontalAlignment="Left"/>

            <TextBlock x:Name="StatusTextBlock" Text="未加载" Margin="0,10,0,0" Foreground="#FF95A5A6" FontSize="12"/>
        </StackPanel>
    </Border>
</UserControl>
