<UserControl x:Class="WpfApp2.UI.Controls.SmartAnalysisAlertPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#FF4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ClearButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#FFDC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFC82333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="Margin" Value="10,10,10,5"/>
        </Style>

        <Style x:Key="StatsTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#FF666666"/>
            <Setter Property="Margin" Value="10,5"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 标题和统计信息 -->
            <RowDefinition Height="Auto"/>
            <!-- 操作按钮区 -->
            <RowDefinition Height="*"/>
            <!-- 告警记录列表 -->
            <RowDefinition Height="Auto"/>
            <!-- 返回按钮区 -->
        </Grid.RowDefinitions>

        <!-- 标题和统计信息 -->
        <Grid Grid.Row="0" Background="#FFF8F9FA" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="🚨 告警记录管理" Style="{StaticResource HeaderTextStyle}"/>
            
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <TextBlock Name="TotalAlertsText" Text="总告警次数: 0" Style="{StaticResource StatsTextStyle}"/>
                <TextBlock Name="RecentAlertsText" Text="今日告警: 0" Style="{StaticResource StatsTextStyle}" Margin="20,5,10,5"/>
                <TextBlock Name="LastAlertTimeText" Text="最后告警: 无" Style="{StaticResource StatsTextStyle}"/>
            </StackPanel>
        </Grid>

        <!-- 操作按钮区 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0,10,10">
            <Button Name="RefreshButton" Content="🔄 刷新" Style="{StaticResource ButtonStyle}" Click="RefreshButton_Click"/>
            <Button Name="ExportButton" Content="📁 导出" Style="{StaticResource ButtonStyle}" Click="ExportButton_Click"/>
            <Button Name="ClearAllButton" Content="🗑 清空记录" Style="{StaticResource ClearButtonStyle}" Click="ClearAllButton_Click"/>
        </StackPanel>

        <!-- 告警记录列表 -->
        <Border Grid.Row="2" BorderBrush="#FFDDDDDD" BorderThickness="1" Margin="10">
            <Grid>
                <DataGrid Name="AlertRecordDataGrid" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#FFF8F9FA"
                          FontSize="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding Timestamp}" Width="140"/>
                        <DataGridTextColumn Header="项目名称" Binding="{Binding ItemName}" Width="120"/>
                        <DataGridTextColumn Header="告警类型" Binding="{Binding AlertType}" Width="120"/>
                        <DataGridTextColumn Header="告警内容" Binding="{Binding AlertMessage}" Width="*"/>
                        <DataGridTextColumn Header="数值详情" Binding="{Binding Details}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 空数据提示 -->
                <TextBlock Name="EmptyMessageText" 
                           Text="🔍 暂无告警记录" 
                           FontSize="16" 
                           Foreground="#FF999999" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Visibility="Collapsed"/>
            </Grid>
        </Border>

        <!-- 返回按钮区 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10">
            <Button Name="BackButton" Content="← 返回" Style="{StaticResource ButtonStyle}" Click="BackButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl> 