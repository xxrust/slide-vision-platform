<UserControl x:Class="WpfApp2.UI.Controls.SmartAnalysisWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:WpfApp2.UI.Controls">
    
    <UserControl.Resources>
        <Style x:Key="ChartButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="#FF4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFF0F0F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <!-- 标题和控制区 -->
            <RowDefinition Height="*"/>
            <!-- 页面内容区 -->
        </Grid.RowDefinitions>
        
        <!-- 标题和控制区 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="质量分析仪表板" FontSize="16" FontWeight="Bold" 
                       VerticalAlignment="Center" Margin="10,0"/>

            <Button Grid.Column="1" x:Name="FloatingModeButton" Content="悬浮模式"
                    Style="{StaticResource ChartButtonStyle}" Width="90" Height="30"
                    VerticalAlignment="Center" Click="FloatingModeButton_Click"/>

            <Button Grid.Column="2" Name="AlertRecordButton" Content="🚨" 
                    Style="{StaticResource IconButtonStyle}" Click="AlertRecordButton_Click" 
                    ToolTip="告警记录"/>

            <Button Grid.Column="3" Name="FocusButton" Content="🎯" 
                    Style="{StaticResource IconButtonStyle}" Click="FocusButton_Click"/>
            
            <Button Grid.Column="4" Name="SettingsButton" Content="⚙" 
                    Style="{StaticResource IconButtonStyle}" Click="SettingsButton_Click"/>
            
            <Button Grid.Column="5" Name="HelpButton" Content="？" 
                    Style="{StaticResource IconButtonStyle}" Click="HelpButton_Click"/>
        </Grid>
        
        <!-- 页面内容区 -->
        <Grid Grid.Row="1" Margin="0">
            <Grid Name="PageContainer">
                <!-- 主页面 -->
                <local:SmartAnalysisMainPage x:Name="MainPage" Visibility="Visible"/>
                
                <!-- 告警记录页面 -->
                <local:SmartAnalysisAlertPage x:Name="AlertPage" Visibility="Collapsed"/>
                
                <!-- 关注项目设置页面 -->
                <local:SmartAnalysisFocusPage x:Name="FocusPage" Visibility="Collapsed"/>
                
                <!-- 设置页面 -->
                <local:SmartAnalysisSettingsPage x:Name="SettingsPage" Visibility="Collapsed"/>
                
                <!-- 帮助页面 -->
                <local:SmartAnalysisHelpPage x:Name="HelpPage" Visibility="Collapsed"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl> 
