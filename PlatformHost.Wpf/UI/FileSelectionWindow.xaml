<Window x:Class="WpfApp2.UI.FileSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文件选择" Height="466" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FF2C3E50">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" 
                   Text="请选择要查看的文件类型" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="White" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>
        
        <!-- 选择按钮区域 -->
        <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Center">
            
            <!-- 实时文档按钮 -->
            <Border Background="#FF3498DB" BorderThickness="1" BorderBrush="#FF2980B9" 
                    Height="50" Margin="0,10" CornerRadius="8" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="315" ShadowDepth="3" Opacity="0.3"/>
                </Border.Effect>
                <Button x:Name="RealTimeDocButton" 
                        Click="RealTimeDocButton_Click"
                        Background="Transparent" 
                        BorderThickness="0" 
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF5DADE2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📊" FontSize="18" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <TextBlock Text="实时文档 (CSV)" FontSize="16" FontWeight="Medium" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Border>
            
            <!-- 图片按钮 -->
            <Border Background="#FF27AE60" BorderThickness="1" BorderBrush="#FF1E8449" 
                    Height="50" Margin="0,10" CornerRadius="8" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="315" ShadowDepth="3" Opacity="0.3"/>
                </Border.Effect>
                <Button x:Name="ImagesButton" 
                        Click="ImagesButton_Click"
                        Background="Transparent" 
                        BorderThickness="0" 
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF2ECC71"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🖼️" FontSize="18" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <TextBlock Text="图片" FontSize="16" FontWeight="Medium" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Border>
            
            <!-- 导出的文档按钮 -->
            <Border Background="#FFF39C12" BorderThickness="1" BorderBrush="#FFD68910" 
                    Height="50" Margin="0,10" CornerRadius="8" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="315" ShadowDepth="3" Opacity="0.3"/>
                </Border.Effect>
                <Button x:Name="ExportDocButton" 
                        Click="ExportDocButton_Click"
                        Background="Transparent" 
                        BorderThickness="0" 
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFF5B041"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📋" FontSize="18" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <TextBlock Text="导出的文档" FontSize="16" FontWeight="Medium" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Border>
            
            <!-- 查看渲染图按钮 -->
            <Border Background="#FF9B59B6" BorderThickness="1" BorderBrush="#FF8E44AD" 
                    Height="50" Margin="0,10" CornerRadius="8" Cursor="Hand">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="315" ShadowDepth="3" Opacity="0.3"/>
                </Border.Effect>
                <Button x:Name="RenderViewerButton" 
                        Click="RenderViewerButton_Click"
                        Background="Transparent" 
                        BorderThickness="0" 
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFBB6BD9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🎨" FontSize="18" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <TextBlock Text="查看渲染图" FontSize="16" FontWeight="Medium" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Border>
            
        </StackPanel>
        
        <!-- 取消按钮 -->
        <Button Grid.Row="2" 
                x:Name="CancelButton" 
                Content="取消" 
                Click="CancelButton_Click"
                Width="100" 
                Height="35" 
                HorizontalAlignment="Center" 
                Background="#FF95A5A6" 
                Foreground="White" 
                BorderThickness="0" 
                FontSize="14" 
                FontWeight="Medium"
                Cursor="Hand"
                Margin="0,20,0,0">
            <Button.Style>
                <Style TargetType="Button">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FFBDC3C7"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
        
    </Grid>
</Window> 