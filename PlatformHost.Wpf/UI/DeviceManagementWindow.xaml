<Window x:Class="WpfApp2.UI.DeviceManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设备管理" Height="600" Width="920"
        WindowStartupLocation="CenterScreen"
        Background="#FF2C3E50">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF34495E" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical" Grid.Column="0">
                    <TextBlock Text="设备管理" FontSize="20" FontWeight="Bold" Foreground="#FF3498DB"/>
                    <TextBlock Text="查看已配置设备与连接状态" FontSize="12" Foreground="#FFBDC3C7" Margin="0,4,0,0"/>
                </StackPanel>
                <Button x:Name="RefreshButton" Grid.Column="1" Content="刷新" Width="90" Height="32"
                        Background="#FF3498DB" Foreground="White" FontWeight="Bold"
                        Click="RefreshButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <DataGrid x:Name="DeviceGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                      HeadersVisibility="Column" IsReadOnly="True" RowHeight="38"
                      Background="#FF34495E" Foreground="White" GridLinesVisibility="None"
                      BorderBrush="#FF2C3E50" BorderThickness="0" SelectionMode="Single">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FF2C3E50"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="8,6"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="#FF34495E"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="AlternationIndex" Value="0"/>
                        <Style.Triggers>
                            <Trigger Property="AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#FF3B536B"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Resources>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,4"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="协议" Binding="{Binding Protocol}" Width="*"/>
                    <DataGridTextColumn Header="地址" Binding="{Binding Address}" Width="2*"/>
                    <DataGridTemplateColumn Header="状态" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="10" Height="10" Fill="{Binding StatusBrush}" Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding StatusText}" ToolTip="{Binding StatusTooltip}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock x:Name="EmptyHint" Text="暂无设备配置" Foreground="#FFBDC3C7" FontSize="14"
                       HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="AddButton" Content="新增" Width="90" Height="32" Margin="0,0,10,0"
                    Background="#FF27AE60" Foreground="White" FontWeight="Bold"
                    Click="AddButton_Click"/>
            <Button x:Name="EditButton" Content="编辑" Width="90" Height="32" Margin="0,0,10,0"
                    Background="#FF3498DB" Foreground="White" FontWeight="Bold"
                    Click="EditButton_Click"/>
            <Button x:Name="DeleteButton" Content="删除" Width="90" Height="32" Margin="0,0,10,0"
                    Background="#FFE74C3C" Foreground="White" FontWeight="Bold"
                    Click="DeleteButton_Click"/>
            <Button x:Name="CloseButton" Content="关闭" Width="90" Height="32"
                    Background="#FF95A5A6" Foreground="White" FontWeight="Bold"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
